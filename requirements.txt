# =============================================================================
# Cortex Trading System — Dependencies
# =============================================================================

# Broker
MetaTrader5>=5.0.45

# Data & numerics
numpy>=1.26.0
pandas>=2.1.0
scipy>=1.11.0

# Technical indicators
pandas-ta>=0.3.14b
TA-Lib>=0.4.28

# Machine Learning — HMM
hmmlearn>=0.3.2

# Machine Learning — Deep Learning
torch>=2.2.0
torchvision>=0.17.0

# Experiment tracking & model registry (T-8)
mlflow>=2.18.0
optuna>=3.5.0

# Meta-labeling & gradient boosting (M-1, M-2)
lightgbm>=4.3.0
scikit-learn>=1.4.0

# NLP / Sentiment
transformers>=4.38.0        # FinBERT
sentencepiece>=0.2.0

# Fundamental data APIs
fredapi>=0.5.1              # FRED macroeconomic data
requests>=2.31.0            # Generic HTTP (CoinGecko, NewsAPI, CFTC COT)
beautifulsoup4>=4.12.0      # HTML parsing for COT scraping

# Cross-asset & alternative data (Wave 7.5 — all free, no API key)
yfinance>=0.2.40            # Yahoo Finance — DXY, VIX, SPX, oil, bonds
pytrends>=4.9.2             # Google Trends search interest

# Database — PostgreSQL async + migrations
sqlalchemy[asyncio]>=2.0.28
asyncpg>=0.29.0              # Non-blocking PostgreSQL driver (primary)
psycopg2-binary>=2.9.9       # Sync fallback + used by Alembic
alembic>=1.13.0              # Schema migration tool (optional but recommended)

# Config & environment
pydantic-settings>=2.2.0
pyyaml>=6.0.1
python-dotenv>=1.0.1

# Scheduling
apscheduler>=3.10.4

# Dashboard API (Wave 12 — FastAPI + React SPA)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
bcrypt>=4.1.0
python-jose[cryptography]>=3.3.0
sse-starlette>=2.1.0

# Testing
pytest>=8.0.0
pytest-mock>=3.12.0

# Utilities
loguru>=0.7.2               # Structured logging
tqdm>=4.66.0                # Progress bars for training
joblib>=1.3.0               # Model serialization
